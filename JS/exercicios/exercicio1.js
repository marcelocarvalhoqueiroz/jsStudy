function operacoes(a, b){
    //+, -, /, *
    let soma = a + b
    let sub = a - b
    let mult = a * b
    let div = a / b
    console.log(`Resultados das operações Soma, Subtração, Multiplicação e Divisão de ${a} e ${b}, respectivamente: ${soma}, ${sub}, ${mult}, ${div}`)
}

operacoes(6, 2)
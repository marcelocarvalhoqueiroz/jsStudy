// Código não executável
// Procedural

processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        // ...
    }
}

objeto.processamento() // Foco passou a ser o objeto

// Principios importantes:
// 1 - Abstração -> Traduzir objetos do mundo real para o código. Como um carro, que possui inúmeros atributos. Depende do contexto do sistema
// 2 - Encapsulamento -> Ter os detalhes de implementação escondidos, mostrar apenas uma interface simples para quem vai interagir com o sistema
// 3 - Herança (prototype) -> Baseado em "é um" || Priorizar composição ao invés de herança. Priorizar tem um ao invés de é um
// 4 - Polimorfismo -> Multiplas formas. Ex: Celular, pode ser uma caralhada de celulares.